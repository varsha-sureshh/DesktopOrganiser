# Desktop File Organizer

A Python automation tool that scans the Desktop, categorizes files by type (Images, Videos, Documents, etc.), 
creates necessary folders automatically, and moves files into them. Also packaged as a standalone .exe using PyInstaller.

## Features
- Detects file types automatically
- Organizes files into folders
- Removes desktop clutter
- EXE version available

## How to Run
